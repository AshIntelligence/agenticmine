# Incident Triage Agent

**Reliability / Agents · runnable synthetic prototype**

Turns noisy symptoms into severity, likely owner and next action before any generative status summary.

```bash
python main.py
python main.py --test
```

**Product point:** deterministic evidence and routing should precede generative explanation during high-pressure incidents.
