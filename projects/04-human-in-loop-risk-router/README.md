# Human-in-the-Loop Risk Router

**Trust / Safety · runnable synthetic prototype**

Routes proposed actions to **ALLOW / REVIEW / DENY** using consequence, confidence, reversibility and data sensitivity.

```bash
python main.py
python main.py --test
```

**Product point:** review everything is safe but unusable; the product needs an explicit, calibrated human-control boundary.
