# Agent vs Workflow Router

**Agent Architecture · runnable synthetic prototype**

Chooses **deterministic workflow / assisted agent / autonomous agent** from task variability, ambiguity, state, tool count and consequence.

```bash
python main.py
python main.py --test
```

**Product point:** agency is a mechanism choice, not a default. High-consequence work reduces autonomy even when the task is variable.
