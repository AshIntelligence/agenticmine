# Telemetry Anomaly → Product Action

**Observability / AI · runnable synthetic prototype**

Detects rolling anomalies and maps them to an explicit product/operational next action.

```bash
python main.py
python main.py --test
```

**Product point:** a red chart is not an outcome. Telemetry becomes valuable when it changes investigation, rollout or product direction.
